#pragma once
#include "AppCore.h"
/*
class ECS;

struct EntityRegistry {
	uint8_t* ComponentIDs{};
	uint8_t ComponentCount{};
	ECS* ComponentSystem{};
};
class Entity {
public:

	template<typename T>
	T* AddComponent();
	template<typename T>
	T* GetComponent();
	template<typename T>
	bool HasComponent();


	//template<typename T>
	//void RemoveComponent()

private:
	EntityRegistry* m_Registry{};
};
class ECS {
public:
	Entity CreateEntity();
	template<typename T>
	T* GetComponent();
	template<typename T>
	T* AddComponent();
private:
	std::vector<>
};
*/